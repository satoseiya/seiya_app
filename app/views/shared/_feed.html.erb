<% if @feed_items.any? %><!--micropostControllerでcreateした時saveできないと@feed_itemsになにも入らず壊れる-->
	<ol class="microposts">
		<%= render partial: 'shared/feed_item', collection: @feed_items %>
	</ol>
	<%= will_paginate @feed_items %>
<% end %>